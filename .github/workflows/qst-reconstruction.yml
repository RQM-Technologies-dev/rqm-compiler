name: QST constrained reconstruction
on:
 push:
  branches: [main]
  paths: ["benchmarks/qst_reconstruction.py",".github/workflows/qst-reconstruction.yml"]
 workflow_dispatch:
permissions: {contents: read}
jobs:
 qst:
  runs-on: ubuntu-latest
  timeout-minutes: 10
  steps:
   - uses: actions/checkout@v4
   - uses: actions/setup-python@v5
     with: {python-version: "3.12", cache: pip}
   - run: python -m pip install -e ".[dev]"
   - run: python benchmarks/qst_reconstruction.py
   - if: always()
     uses: actions/upload-artifact@v4
     with: {name: "qst-reconstruction-${{ github.run_id }}", path: "results/", if-no-files-found: error}
